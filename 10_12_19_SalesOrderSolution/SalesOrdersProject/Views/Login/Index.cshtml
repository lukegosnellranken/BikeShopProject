@model SalesOrdersProject.Models.Customer

@{
    ViewBag.Title = "Login User";
}

<h3>Login:</h3>

@using (Html.BeginForm("Index", "Login"))
{
    @Html.Hidden("returnUrl", Request.Url.PathAndQuery)

    @Html.ValidationSummary(true, "Login failed.  Check login details")

    <div id="loginScreen">
        <div class="form-group">
            <div class="col-xs-4">
                <label>Email Address</label>
                @Html.TextBoxFor(x => x.CustomerEmailAddress, new { @class = "form-control" })
            </div>
        </div>
    </div>

    @*<div class="form-group">
            <label>Password</label>
            @Html.PasswordFor(x => x.CustomerPassword, new { @class = "form-control" })
            @Html.TextBoxFor(x => x.CustomerPassword, new { @class = "form-control" })
        </div>*@

    <div id="loginSubmit">
        <div class="loginSubmit text-center">
            <input class="btn btn-primary" type="submit" value="Login" />
        </div>
    </div>
}

